{
  "name": "tokra-use-cors",
  "version": "0.0.1-rc.2",
  "description": "Tokra hook for CORS request/response",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "author": "rayepps",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "node --require ts-node/register --test **/*.test.ts",
    "clean": "rm -rf ./node_modules && rm -rf ./dist && rm -rf ./coverage"
  },
  "dependencies": {
    "tokra": "^0.0.1-rc.3"
  }
}
