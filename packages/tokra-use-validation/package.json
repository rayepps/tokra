{
  "name": "tokra-use-validation",
  "version": "0.0.1-rc.0",
  "description": "Tokra request validation hooks",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "author": "rayepps",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "node --require ts-node/register --test **/*.test.ts",
    "clean": "rm -rf ./node_modules && rm -rf ./dist && rm -rf ./coverage"
  },
  "devDependencies": {},
  "dependencies": {
    "tokra": "^0.0.1-rc.1",
    "yup": "^0.32.11"
  }
}
