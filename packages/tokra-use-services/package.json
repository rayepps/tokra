{
  "name": "tokra-use-services",
  "version": "0.0.1-rc.0",
  "description": "Tokra hook to inject services into function",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "author": "rayepps",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "node --require ts-node/register --test **/*.test.ts",
    "clean": "rm -rf ./node_modules && rm -rf ./dist && rm -rf ./coverage"
  },
  "devDependencies": {},
  "dependencies": {
    "radash": "^7.1.0",
    "tokra": "^0.0.1-rc.1"
  }
}
