{
  "name": "tokra-use-lambda",
  "version": "0.0.1-rc.0",
  "description": "Tokra root hook to handle function running on AWS Lambda",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "author": "rayepps",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "node --require ts-node/register --test **/*.test.ts",
    "clean": "rm -rf ./node_modules && rm -rf ./dist && rm -rf ./coverage"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.97"
  },
  "dependencies": {
    "tokra": "^0.0.1-rc.1"
  }
}