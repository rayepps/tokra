{
  "name": "tokra-use-jwt-auth",
  "version": "0.0.1-rc.1",
  "description": "Tokra authentication hook for JWTs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "author": "rayepps",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "node --require ts-node/register --test **/*.test.ts",
    "clean": "rm -rf ./node_modules && rm -rf ./dist && rm -rf ./coverage"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^8.5.6"
  },
  "dependencies": {
    "jsonwebtoken": "^8.5.1",
    "tokra": "^0.0.1-rc.2"
  }
}
