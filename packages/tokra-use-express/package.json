{
  "name": "tokra-use-express",
  "version": "0.0.1-rc.1",
  "description": "Tokra root hook to handle function running on Express",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "author": "rayepps",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "node --require ts-node/register --test **/*.test.ts",
    "clean": "rm -rf ./node_modules && rm -rf ./dist && rm -rf ./coverage"
  },
  "peerDependencies": {
    "express": "^4.18.1"
  },
  "devDependencies": {
    "@types/compression": "^1.7.2",
    "@types/express": "^4.17.13",
    "express": "^4.18.1"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "tokra": "^0.0.1-rc.2"
  }
}
